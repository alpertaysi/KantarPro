<Window x:Class="KantarPro.Desktop.PaymentTypeChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ödeme Türü"
        Width="390"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI">
    <Border Background="#FFFFFF"
            BorderBrush="#CBD5E1"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22"
                              ShadowDepth="4"
                              Direction="270"
                              Opacity="0.22" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="6" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Background="#0F766E" CornerRadius="8,8,0,0" />

            <StackPanel Grid.Row="1" Margin="22,20,22,18">
                <TextBlock Text="Ödeme türünü seçin"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#0F172A"
                           Margin="0,0,0,8" />
                <TextBlock Text="Bu seçim tahsilat kaydına, günlük hasılata ve işlem detayına yazılacak."
                           TextWrapping="Wrap"
                           FontSize="13"
                           Foreground="#475569" />
            </StackPanel>

            <Border Grid.Row="2"
                    Background="#F8FAFC"
                    BorderBrush="#E2E8F0"
                    BorderThickness="0,1,0,0"
                    Padding="14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Nakit"
                            Width="112"
                            Height="36"
                            Margin="0,0,8,0"
                            Background="#0F766E"
                            BorderBrush="#115E59"
                            Foreground="White"
                            FontWeight="SemiBold"
                            IsDefault="True"
                            Click="CashButton_Click" />
                    <Button Content="Kredi Kartı"
                            Width="122"
                            Height="36"
                            Margin="0,0,8,0"
                            Background="#1D4ED8"
                            BorderBrush="#1E40AF"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Click="CardButton_Click" />
                    <Button Content="Vazgec"
                            Width="94"
                            Height="36"
                            Background="#E2E8F0"
                            BorderBrush="#CBD5E1"
                            IsCancel="True"
                            Click="CancelButton_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
