<Window x:Class="KantarPro.Desktop.ChangePasswordWindow"
        Icon="Assets/kantarpro.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Şifre Değiştir"
        Width="480"
        Height="460"
        MinWidth="480"
        MinHeight="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0F766E" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#0B5E58" />
        <Style x:Key="PasswordInputStyle" TargetType="{x:Type PasswordBox}">
            <Setter Property="Height" Value="38" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="BorderBrush" Value="#CBD5E1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="38" />
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="18" Background="#FFFFFF" BorderBrush="#D6E1EA" BorderThickness="1" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Şifre Değiştir" FontSize="22" FontWeight="SemiBold" Foreground="#0F172A" />
                <TextBlock x:Name="UserInfoTextBlock"
                           Text="Kullanıcı"
                           Foreground="#64748B"
                           Margin="0,6,0,0" />
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,24,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="142" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Mevcut Şifre" VerticalAlignment="Center" Margin="0,0,12,14" />
                <PasswordBox x:Name="CurrentPasswordBox" Grid.Column="1" Style="{StaticResource PasswordInputStyle}" Margin="0,0,0,14" />

                <TextBlock Grid.Row="1" Text="Yeni Şifre" VerticalAlignment="Center" Margin="0,0,12,14" />
                <PasswordBox x:Name="NewPasswordBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource PasswordInputStyle}" Margin="0,0,0,14" />

                <TextBlock Grid.Row="2" Text="Yeni Şifre Tekrar" VerticalAlignment="Center" Margin="0,0,12,0" />
                <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="2" Grid.Column="1" Style="{StaticResource PasswordInputStyle}" />
            </Grid>

            <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" Margin="0,18,0,0">
                <TextBlock Text="Bu işlem yalnızca oturum açan kullanıcının şifresini değiştirir."
                           Foreground="#475569"
                           TextWrapping="Wrap" />
            </Border>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                <Button Content="Vazgeç" Width="100" Height="38" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="Kaydet" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
